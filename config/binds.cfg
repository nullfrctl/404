// 404 --- a simple tf2 config --- binds

// movement
bind "w" "+mf"
bind "a" "+ml"
bind "s" "+mb"
bind "d" "+mr"

// team fortress
bind "h" "+inspect"
bind "f" "+use_action_slot_item"

// miscellaneous
bind "F1" "apply_overrides"

// print success message
echo "404 --- binds loaded"
